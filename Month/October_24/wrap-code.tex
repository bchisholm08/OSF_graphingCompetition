\usepackage{listings}

\lstset{
    breaklines=true,        % Automatically break long lines
    breakatwhitespace=true, % Break at whitespace if possible
    basicstyle=\ttfamily,   % Use a monospaced font for code
    columns=fullflexible,   % Improve line-wrapping behavior
    keepspaces=true,        % Keep indentation
    numbers=left,           % Add line numbers to the left
    numberstyle=\tiny,      % Line number style
    showstringspaces=false, % Hide spaces in strings
    frame=single,           % Add a frame around code
    xleftmargin=15pt,       % Left margin for code readability
    language=R              % Set syntax highlighting to R
}
